# Streamlit Frontend Best Practices & Improvement Guide

เอกสารนี้รวบรวมแนวทางปฏิบัติที่ดีที่สุด (Best Practices) สำหรับการพัฒนา Frontend ด้วย Streamlit โดยเน้นที่ประสิทธิภาพ (Performance), ความสวยงาม (Aesthetics), และประสบการณ์ผู้ใช้งาน (User Experience - UX) พร้อมคำแนะนำเฉพาะสำหรับโปรเจกต์ **Roo-Lot**

---

## 1. Layout & Structure (การจัดวางและโครงสร้าง)

การจัด Layout ที่ดีช่วยให้ผู้ใช้งานเข้าใจข้อมูลได้ง่ายและดูเป็นมืออาชีพ

### 1.1 ใช้ Containers และ Columns อย่างมีระบบ
แทนที่จะวาง Widget ต่อกันลงมาเรื่อยๆ ควรใช้การจัดกลุ่ม:
- **`st.container(border=True)`**: (แนะนำสำหรับ Streamlit 1.30+) ใช้สร้างกรอบเพื่อแบ่งส่วนเนื้อหาให้ชัดเจน เช่น แยกส่วน Input และ Output
- **`st.columns`**: ใช้จัดวางข้อมูลแนวนอน แต่อย่าใช้เยอะเกินไป (แนะนำ 2-3 คอลัมน์สำหรับ Desktop)
- **`st.expander`**: ซ่อนข้อมูลที่ไม่จำเป็นต้องเห็นทันที เช่น "Advanced Options" หรือ "Technical Details" (Roo-Lot ใช้อยู่แล้ว ซึ่งดีมาก)

### 1.2 Sidebar Management
- **`st.sidebar`** สำหรับ Navigation และ Global Settings เท่านั้น
- หลีกเลี่ยงการใส่ Input ที่ส่งผลต่อผลลัพธ์หลักไว้ใน Sidebar หากหน้าจอหลักมีพื้นที่เพียงพอ เพราะผู้ใช้อาจมองข้าม

### 1.3 Tabs for Context
- ใช้ **`st.tabs`** เมื่อต้องการนำเสนอข้อมูลชุดเดียวกันในหลายมุมมอง (เช่น Tab 1: กราฟ, Tab 2: ตารางข้อมูล, Tab 3: คำอธิบาย)

---

## 2. Performance & Optimization (ประสิทธิภาพ)

Streamlit จะรัน Code ใหม่ทั้งหมด (Rerun) ทุกครั้งที่มีการ Interaction การจัดการ Performance จึงสำคัญมาก

### 2.1 Caching (หัวใจสำคัญ)
- **`@st.cache_resource`**: ใช้สำหรับ Objects ที่โหลดนานและกินแรมเยอะ เช่น Machine Learning Models หรือ Database Connections (Roo-Lot ใช้แล้วกับ `load_model`)
- **`@st.cache_data`**: ใช้สำหรับ Data Manipulations, API Calls หรือการอ่านไฟล์ CSV/Excel ที่ได้ผลลัพธ์เดิมเสมอถ้า Input เหมือนเดิม

### 2.2 Partial Reruns (ฟีเจอร์ใหม่!)
- **`@st.fragment`**: (Streamlit 1.37+) ช่วยให้ Function บางส่วนรันใหม่ได้โดย *ไม่ต้อง* Rerun ทั้งหน้า
    - **Use Case**: ปุ่ม "Refresh" ข้อมูลหุ้น หรือการอัปเดตกราฟแบบ Real-time
    - **ใน Roo-Lot**: สามารถใช้กับส่วน `Prediction History` ใน Sidebar ได้ เพื่อให้มันอัปเดตโดยไม่กระทบหน้าจอหลัก

### 2.3 Session State management
- ใช้ `st.session_state` เพื่อจำค่าตัวแปรระหว่างการ Rerun (Roo-Lot ทำได้ดีในการเก็บ History)
- หลีกเลี่ยงการคำนวณหนักๆ ใน Global Scope ควรยัดใส่ Function และ Cache ไว้

---

## 3. User Experience (UX) & Aesthetics (ความสวยงาม)

ทำให้แอปรู้สึก "พรีเมียม" และใช้งานง่าย

### 3.1 Feedback & Status
- **`st.toast`**: ใช้แจ้งเตือนแบบชั่วคราว (Ephemeral) เช่น "บันทึกข้อมูลสำเร็จ" แทนที่จะใช้ `st.success` ที่ค้างอยู่บนหน้าจอ
- **`st.status` / `st.spinner`**: แสดงสถานะเมื่อมีการประมวลผลนานกว่า 1 วินาที (Roo-Lot ใช้ `track_time` สำหรับ debug แต่ User ไม่เห็น ควรเพิ่ม `with st.spinner('Calculating...'):`)

### 3.2 Modern Widgets
- **`st.pills`**: (New) ใช้แทน `st.radio` หรือ `st.multiselect` สำหรับตัวเลือกสั้นๆ น้อยๆ เช่น "School Break: Yes/No" จะดูทันสมัยกว่า
- **`st.data_editor`**: หากต้องการให้ผู้ใช้แก้ไขข้อมูลในตารางได้ (เช่น แก้ไข History) ให้ใช้ตัวนี้แทน `st.dataframe`

### 3.3 Custom Styling (Theming)
- **`.streamlit/config.toml`**: ตั้งค่า Primary Color, Background, Font ให้ตรงกับ Brand
- **Custom CSS**: ใช้ `st.markdown('<style>...</style>', unsafe_allow_html=True)` เพื่อปรับแต่งสิ่งที่ Config ทำไม่ได้ (เช่น ลบ Padding, เปลี่ยนสี Scrollbar) *แต่ควรระวังเพราะอาจพังเมื่ออัปเดต Streamlit*

---

## 4. Visualizations (การแสดงผลข้อมูล)

- **Plotly**: แนะนำที่สุดสำหรับ Interactive Charts (Zoom, Pan, Hover ได้)
- **Vega-Altair**: ดีสำหรับ Declarative Statistical Visualization
- **Streamlit Native Charts**: (`st.line_chart`, `st.bar_chart`) เร็วและง่าย แต่ปรับแต่งได้น้อย
- **Metrics**: ใช้ `st.metric` พร้อม `delta` สีเขียว/แดง เพื่อแสดงการเปลี่ยนแปลง (Roo-Lot ใช้อยู่แล้ว ดีเยี่ยม)

---

## 5. Micro Animations (การเคลื่อนไหวเล็กๆ ที่สร้างความแตกต่าง)

Micro Animations ช่วยให้เว็บดูมีชีวิตชีวาและตอบสนองผู้ใช้ได้ดียิ่งขึ้น

### 5.1 CSS Micro-interactions (Button & Card Hover)
ใช้ CSS เพื่อเพิ่มลูกเล่นเมื่อเอาเมาส์ไปชี้ (Hover) บนปุ่มหรือ Container 
```python
st.markdown("""
<style>
/* Button Hover Effect */
div.stButton > button:first-child:hover {
    transform: scale(1.02);
    box-shadow: 0px 4px 12px rgba(0,0,0,0.1);
    transition: all 0.2s ease-in-out;
}
/* Card/Container Hover Effect */
[data-testid="stVerticalBlock"] > [style*="flex-direction: column;"] > [data-testid="stVerticalBlock"] {
    transition: all 0.3s ease;
}
[data-testid="stVerticalBlock"] > [style*="flex-direction: column;"] > [data-testid="stVerticalBlock"]:hover {
    box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
    transform: translateY(-2px);
}
</style>
""", unsafe_allow_html=True)
```

### 5.2 Lottie Animations (แนะนำสำหรับ Loading / Success)
ใช้ห้องสมุด `streamlit-lottie` เพื่อแสดง Animation แบบ JSON ที่เบาและคมชัด
- **Use Case**: แสดงรูปคนกำลังนั่งคิดตอน `model.predict()` หรือแสดงพลุตอนทำนายเสร็จ
- **Installation**: `pip install streamlit-lottie`

```python
from streamlit_lottie import st_lottie
import requests

def load_lottieurl(url: str):
    r = requests.get(url)
    if r.status_code != 200:
        return None
    return r.json()

# ตัวอย่างการใช้งาน
lottie_loading = load_lottieurl("https://assets5.lottiefiles.com/packages/lf20_t9gkkhz4.json")
st_lottie(lottie_loading, speed=1, height=200, key="loading")
```

### 5.3 Native Streamlit Effects
ใช้ Effect ที่มีให้อยู่แล้วอย่างเหมาะสม:
- **`st.balloons()`**: ใช้เมื่อทำภารกิจสำเร็จ (เช่น บันทึกข้อมูลเสร็จ) แต่ใช้นานๆ ครั้งพอ
- **`st.snow()`**: ใช้ตามเทศกาลหรือธีม

---

## 6. Specific Recommendations for Roo-Lot (ข้อเสนอแนะปรับปรุง)

จากการวิเคราะห์ `app.py` ปัจจุบัน ขอเสนอแนวทางปรับปรุงดังนี้:

| Feature | Current State | Suggested Improvement |
| :--- | :--- | :--- |
| **Model Loading** | ใช้ `track_time` (Console log) | ใช้ `st.status` หรือ `st.spinner` ให้ผู้ใช้เห็นว่ากำลังโหลด |
| **Input Grouping** | ใช้ `st.columns` ธรรมดา | ห่อ `st.columns` ด้วย `st.container(border=True)` เพื่อสร้างกรอบให้ส่วน Input ดูเด่นชัดขึ้น |
| **School Break Input** | ใช้ `st.radio` | เปลี่ยนเป็น **`st.pills`** (ถ้าอัปเดต Streamlit) หรือ `st.toggle` เพื่อความ Minimal |
| **Results Display** | แสดง Text และ Metrics | เพิ่ม **Gauge Chart** (Plotly) แสดงระดับค่าไฟ (Low/Moderate/High) เพื่อความสวยงาม |
| **History Table** | `st.dataframe` แบบ Static | ใช้ **`st.data_editor`** (disabled editing) เพื่อหน้าตาที่ Clean กว่าและรองรับ Interaction |
| **Notifications** | `st.success` | ใช้ **`st.toast`** เมื่อทำนายเสร็จ เพื่อไม่ให้รกหน้าจอ |

### Code Example: Modern Input Section
```python
with st.container(border=True):
    st.subheader("📝 Usage Details")
    col1, col2 = st.columns(2)
    with col1:
        current = st.number_input("Current Month (kWh)", 0, 2000)
    with col2:
        prev = st.number_input("Previous Month (kWh)", 0, 2000)
    
    # Modern Toggle for Binary Choice
    is_break = st.toggle("School Break Period?")
```
