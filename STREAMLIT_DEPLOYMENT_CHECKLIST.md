# 🚀 Streamlit Pre-deployment Checklist (รายการตรวจสอบก่อนขึ้นระบบจริง)

เอกสารนี้รวบรวมรายการที่ต้องตรวจสอบก่อนนำ **Roo-Lot Chatbot Application** ขึ้นสู่ผู้ให้บริการ Cloud (เช่น Streamlit Community Cloud, Heroku, หรือ Render) เพื่อให้มั่นใจว่าแอปพลิเคชันจะทำงานได้อย่างราบรื่น

---

## 1. 📦 Dependencies & Environment
### ✅ `requirements.txt`
- [ ] มีไฟล์ `requirements.txt` อยู่ที่ root directory
- [ ] ระบุ version ของ library ทั้งหมดชัดเจน (เช่น `streamlit==1.28.0` แทนที่จะเป็น `streamlit`) เพื่อป้องกันปัญหา version mismatch ในอนาคต
- [ ] ลบ library ที่ไม่ใช้ออก (เพื่อลดขนาด build และเวลา install)
- [ ] ตรวจสอบว่ารวม library พื้นฐานครบแล้ว:
  - `streamlit`
  - `pandas`, `numpy`, `scikit-learn` (สำหรับ ML Model)
  - `joblib` (สำหรับโหลด Model)
  - `plotly` (สำหรับกราฟ)

### ✅ `runtime.txt` (สำหรับ Heroku/Render)
- [ ] ตรวจสอบว่าระบุ Python version ที่เข้ากันได้กับ local environment
- [ ] ตัวอย่าง: `python-3.9.18` (ควรตรงกับ version ที่ใช้พัฒนา)

### ✅ `packages.txt` (ถ้ามี)
- [ ] หากมีการใช้ OS-level dependencies (เช่น `libgl1` สำหรับ opencv) ต้องสร้างไฟล์นี้

---

## 2. ⚙️ Configuration & Entry Point
### ✅ Entry Point
- [ ] ตรวจสอบชื่อไฟล์หลัก (App File): **`app_chatbot.py`**
- [ ] หากใช้ **Streamlit Cloud**: ตั้งค่า "Main file path" ให้ตรงกับ `app_chatbot.py`
- [ ] หากใช้ **Heroku**: ตรวจสอบไฟล์ `Procfile`
  ```text
  web: streamlit run app_chatbot.py --server.port=$PORT --server.address=0.0.0.0
  ```

### ✅ `.streamlit/config.toml`
- [ ] ตรวจสอบ Theme setting (สี, font) ว่าถูกต้อง
- [ ] ปิด `client.showErrorDetails = false` หากไม่ต้องการให้ user เห็น error stack trace
- [ ] ตั้งค่า `server.maxUploadSize` หากมีการอัปโหลดไฟล์ขนาดใหญ่

---

## 3. 🔐 Security & Secrets
### ✅ Secrets Management
- [ ] **ห้าม** commit ไฟล์ `.streamlit/secrets.toml` ขึ้น Git (ต้องอยู่ใน `.gitignore`)
- [ ] ไปตั้งค่า Secrets ใน Dashboard ของ Cloud Provider (Streamlit Cloud Settings หรือ Heroku Config Vars)
- [ ] ตรวจสอบว่าไม่มี API Keys หรือ Password ฝัง (Hardcoded) อยู่ในโค้ด Python

### ✅ `.gitignore`
- [ ] ตรวจสอบว่าไฟล์เหล่านี้ถูก ignore แล้ว:
  - `*.pyc`, `__pycache__/`
  - `.env`, `secrets.toml`
  - `venv/`, `.venv/`
  - ไฟล์ข้อมูลส่วนตัว หรือไฟล์ชั่วคราวที่ไม่เกี่ยวข้อง

---

## 4. 💾 Data & Models
### ✅ File Paths
- [ ] เปลี่ยน Absolute Path (เช่น `C:/Users/...`) ให้เป็น **Relative Path** ทั้งหมด
  - ❌ `pandas.read_csv("C:/Users/com/project/data/file.csv")`
  - ✅ `pandas.read_csv("data/file.csv")` หรือใช้ `pathlib`
- [ ] ตรวจสอบว่าโฟลเดอร์ `models/` และไฟล์ `.pkl` ถูก commit ขึ้น Git แล้ว (หากไฟล์ไม่ใหญ่เกิน git limit 100MB)
- [ ] หากไฟล์ Model ใหญ่กว่า 100MB ต้องใช้ Git LFS หรือโหลดจาก External Storage (S3/GDrive)

### ✅ Caching
- [ ] ตรวจสอบการใช้ `@st.cache_resource` สำหรับโหลด Model (โหลดครั้งเดียว ไม่โหลดใหม่ทุก interaction)
  ```python
  @st.cache_resource
  def load_model():
      return joblib.load('models/lasso_model.pkl')
  ```
- [ ] ตรวจสอบการใช้ `@st.cache_data` สำหรับโหลด Data (CSV/Static files)

---

## 5. 🧪 Testing & Performance
### ✅ Local Test
- [ ] ทดลองรันใน Clean Environment (สร้าง venv ใหม่แล้ว pip install จาก requirements.txt) ว่ารันผ่านหรือไม่
- [ ] ทดสอบฟีเจอร์หลัก: ส่งข้อความ, รับค่าทำนาย, ดาวน์โหลด PDF, เปลี่ยนหน้า

### ✅ Debugging
- [ ] ลบ `print()` หรือ `st.write()` ที่ใช้ debug ออกจากหน้าจอ UI
- [ ] ตรวจสอบว่าไม่มี Error สีแดง (Traceback) แสดงบนหน้าจอเมื่อกดปุ่มต่างๆ รัวๆ

---

## 6. 🚀 Deployment Steps (ขั้นตอนคร่าวๆ)

### สำหรับ Streamlit Community Cloud
1. Push โค้ดล่าสุดขึ้น GitHub
2. ไปที่ [share.streamlit.io](https://share.streamlit.io)
3. กด **New App**
4. เลือก Repository, Branch, และ Main file path (`app_chatbot.py`)
5. กด **Deploy**
6. เข้าไปตั้งค่า Secrets (ถ้ามี) ในปุ่ม Settings ของแอป

### สำหรับ Heroku
1. ติดตั้ง Heroku CLI
2. `heroku login`
3. `heroku create roo-lot-app`
4. `git push heroku master`
5. รอจน Build เสร็จและเปิดเว็บตรวจสอบ

---

**Last Updated:** 2024-02-12
